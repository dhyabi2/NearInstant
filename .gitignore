target/

# Local secrets — test wallet keys live here, NEVER commit.
# .env.example is the committed, value-free template.
.env
.env.*
!.env.example
web/pkg/
web/pkg-xmr/
# The Hermes CLI needs the two swap-core pkg-node wasm builds at runtime, so
# they are COMMITTED (a clone runs with only `node`, no Rust/wasm-pack).
# Regenerated by `wasm-pack build --target nodejs`. Other pkg-node dirs, if any,
# should be added explicitly.

# internal ops runbook — never commit
DEPLOY.internal.md
*.internal.md

# audit findings — keep OUT of the repo (temporary, local only)
AuditTask.md
design/e2e-coverage.md
*.local.md

# economics modelling results — local only, do not publish
*earn-economics*
docs/EARN-ECONOMICS.md
.DS_Store
.xnoxmr-pricehist.json
.xnoxmr-agent.json
.xnoxmr-agent.json.lock
.hw-send/
send_to_agent.cjs
